# One of 'breaking', 'deprecation', 'new_component', 'enhancement', 'bug_fix'
change_type: enhancement

# The name of the component, or a single word describing the area of concern, (e.g. crosslink)
component: processor/rolling_span_latency

# A brief description of the change.  Surround your text with quotes ("") if it needs to start with a backtick (`).
note: Replace the temporary local `rolling_span_latency` processor with the now-upstreamed [rollingspanlatencyprocessor](https://github.com/open-telemetry/opentelemetry-collector-contrib/tree/main/processor/rollingspanlatencyprocessor) from opentelemetry-collector-contrib.

# One or more tracking issues related to the change
issues: [7783]

# (Optional) One or more lines of additional information to render under the primary note.
# These lines will be padded with 2 spaces and then inserted directly into the document.
# Use pipe (|) for multiline entries.
subtext: |
  No configuration or behavior changes; the processor's `rolling_span_latency` type, config schema, and telemetry
  are unchanged. The local `pkg/processor/rollingspanlatencyprocessor` module has been removed.
