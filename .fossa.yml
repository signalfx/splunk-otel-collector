version: 3

targets:
  only:
    - type: gomod
    - type: setuptools
      path: packaging/bundle/python
    - type: setuptools
      path: packaging/bundle/scripts

paths:
  exclude:
    - examples
    - tests
