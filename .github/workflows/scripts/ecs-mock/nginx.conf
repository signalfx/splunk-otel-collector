server {
    listen 80;
    default_type application/json;

    location = /task       { alias /data/task_metadata.json; }
    location = /task/stats { alias /data/task_stats.json; }
    location = /           { alias /data/task_metadata.json; }
}
