server {
    listen 80;
    default_type application/json;
    root /data;

    location = /task       { try_files /task_metadata.json =404; }
    location = /task/stats { try_files /task_stats.json =404; }
    location = /           { try_files /container_metadata.json =404; }
}
