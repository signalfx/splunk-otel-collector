fixtures:
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
      ref: "4.24.0"
    apt:
      repo: "puppetlabs/apt"
      ref: "7.0.0"
    yum:
      repo: "puppet/yum"
      ref: "4.3.0"
    powershell:
      repo: "puppetlabs/powershell"
      ref: "5.0.0"
    registry:
      repo: "puppetlabs/registry"
      ref: "4.0.0"
