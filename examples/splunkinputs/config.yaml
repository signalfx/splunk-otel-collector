receivers:
  splunk_inputs:
    base_dir: /var/splunk_home

exporters:
  splunk_hec:
    endpoint: http://splunk:8088/services/collector/event
    token: 00000000-0000-0000-0000-0000000000000

service:
  pipelines:
    logs:
      receivers: [splunk_inputs]
      exporters: [splunk_hec]