discovery:
  watch_observers:
    - an_observer
  receivers:
    a_receiver:
      rule: type == "container"
      status:
        metrics:
          - status: unsupported
            regexp: '.*'
        statements:
          - status: another_unsupported
            regexp: '.*'
