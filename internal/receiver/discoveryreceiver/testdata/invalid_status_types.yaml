discovery:
  watch_observers:
    - an_observer
  receivers:
    a_receiver:
      rule: a rule
      status:
        metrics:
          unsupported:
            - regexp: '.*'
        statements:
          another_unsupported:
            - regexp: '.*'
