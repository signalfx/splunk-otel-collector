discovery:
  watch_observers:
    - an_observer
  receivers:
    a_receiver:
      rule: a rule
      status:
        metrics:
          - status: unsupported
            regexp: '.*'
        statements:
          - status: another_unsupported
            regexp: '.*'
