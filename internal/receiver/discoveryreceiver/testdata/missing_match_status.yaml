discovery:
  watch_observers:
    - an_observer
  receivers:
    a_receiver:
      service_type: a_service
      rule: type == "container"
      status:
        metrics:
          - regexp: '.*'
        statements:
          - regexp: '.*'
