discovery:
  watch_observers:
    - an_observer
  receivers:
    receiver_creator/with-name:
      service_type: a_service
      rule: type == "container"
      status:
        metrics:
          - status: successful
            regexp: '.*'
