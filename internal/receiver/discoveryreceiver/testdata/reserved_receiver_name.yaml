discovery:
  watch_observers:
    - an_observer
  receivers:
    a_receiver/with-receiver_creator/in-name:
      service_type: a_service
      rule: type == "container"
      status:
        metrics:
          - status: successful
            regexp: '.*'
