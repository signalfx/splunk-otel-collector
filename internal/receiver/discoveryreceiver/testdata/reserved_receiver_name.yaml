discovery:
  watch_observers:
    - an_observer
  receivers:
    a_receiver/with-receiver_creator/in-name:
      rule: a rule
      status:
        metrics:
          successful:
            - regexp: '.*'
