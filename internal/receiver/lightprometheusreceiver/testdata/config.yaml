lightprometheus:
    endpoint: "http://localhost:9090/metrics"
    collection_interval: 10s
    resource_attributes:
      service.name:
        enabled: false
      service.instance.id:
        enabled: false
      server.address:
        enabled: true
