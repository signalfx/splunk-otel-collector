# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for nutanix metrics.
    type: object
    properties:
      nutanix.cluster.count:
        description: "NutanixClusterCountMetricConfig provides config for the nutanix.cluster.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.cluster.info:
        description: "NutanixClusterInfoMetricConfig provides config for the nutanix.cluster.info metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.cluster.stat:
        description: "NutanixClusterStatMetricConfig provides config for the nutanix.cluster.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.data_protection.entity.count:
        description: "NutanixDataProtectionEntityCountMetricConfig provides config for the nutanix.data_protection.entity.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.disk.count:
        description: "NutanixDiskCountMetricConfig provides config for the nutanix.disk.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.disk.stat:
        description: "NutanixDiskStatMetricConfig provides config for the nutanix.disk.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.entity.count:
        description: "NutanixFilesEntityCountMetricConfig provides config for the nutanix.files.entity.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.entity.stat:
        description: "NutanixFilesEntityStatMetricConfig provides config for the nutanix.files.entity.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.host.count:
        description: "NutanixHostCountMetricConfig provides config for the nutanix.host.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.host.stat:
        description: "NutanixHostStatMetricConfig provides config for the nutanix.host.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.microseg.entity.count:
        description: "NutanixMicrosegEntityCountMetricConfig provides config for the nutanix.microseg.entity.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.monitoring.entity.count:
        description: "NutanixMonitoringEntityCountMetricConfig provides config for the nutanix.monitoring.entity.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.entity.count:
        description: "NutanixNetworkingEntityCountMetricConfig provides config for the nutanix.networking.entity.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.entity.stat:
        description: "NutanixNetworkingEntityStatMetricConfig provides config for the nutanix.networking.entity.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.objects.entity.count:
        description: "NutanixObjectsEntityCountMetricConfig provides config for the nutanix.objects.entity.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.objects.entity.stat:
        description: "NutanixObjectsEntityStatMetricConfig provides config for the nutanix.objects.entity.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.prism.entity.count:
        description: "NutanixPrismEntityCountMetricConfig provides config for the nutanix.prism.entity.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.storage.container.count:
        description: "NutanixStorageContainerCountMetricConfig provides config for the nutanix.storage.container.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.storage.container.stat:
        description: "NutanixStorageContainerStatMetricConfig provides config for the nutanix.storage.container.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.subnet.count:
        description: "NutanixSubnetCountMetricConfig provides config for the nutanix.subnet.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.count:
        description: "NutanixVMCountMetricConfig provides config for the nutanix.vm.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.disk.count:
        description: "NutanixVMDiskCountMetricConfig provides config for the nutanix.vm.disk.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.memory.assigned:
        description: "NutanixVMMemoryAssignedMetricConfig provides config for the nutanix.vm.memory.assigned metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.nic.count:
        description: "NutanixVMNicCountMetricConfig provides config for the nutanix.vm.nic.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.stat:
        description: "NutanixVMStatMetricConfig provides config for the nutanix.vm.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.vcpu.count:
        description: "NutanixVMVcpuCountMetricConfig provides config for the nutanix.vm.vcpu.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.volume_group.count:
        description: "NutanixVolumeGroupCountMetricConfig provides config for the nutanix.volume_group.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.volume_group.stat:
        description: "NutanixVolumeGroupStatMetricConfig provides config for the nutanix.volume_group.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for nutanix resource attributes.
    type: object
    properties:
      nutanix.prism.api.version:
        description: ResourceAttributeConfig provides common config for a nutanix.prism.api.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      server.address:
        description: ResourceAttributeConfig provides common config for a server.address resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      server.port:
        description: ResourceAttributeConfig provides common config for a server.port resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.name:
        description: ResourceAttributeConfig provides common config for a service.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for nutanix metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
