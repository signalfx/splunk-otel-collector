# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for nutanix metrics.
    type: object
    properties:
      nutanix.cluster.count:
        description: "NutanixClusterCountMetricConfig provides config for the nutanix.cluster.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.cluster.info:
        description: "NutanixClusterInfoMetricConfig provides config for the nutanix.cluster.info metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.cluster.stat:
        description: "NutanixClusterStatMetricConfig provides config for the nutanix.cluster.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.data_protection.protected_vm.count:
        description: "NutanixDataProtectionProtectedVMCountMetricConfig provides config for the nutanix.data_protection.protected_vm.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.data_protection.protection_policy.count:
        description: "NutanixDataProtectionProtectionPolicyCountMetricConfig provides config for the nutanix.data_protection.protection_policy.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.data_protection.protection_policy_schedule.count:
        description: "NutanixDataProtectionProtectionPolicyScheduleCountMetricConfig provides config for the nutanix.data_protection.protection_policy_schedule.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.data_protection.recovery_point.count:
        description: "NutanixDataProtectionRecoveryPointCountMetricConfig provides config for the nutanix.data_protection.recovery_point.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.disk.count:
        description: "NutanixDiskCountMetricConfig provides config for the nutanix.disk.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.disk.stat:
        description: "NutanixDiskStatMetricConfig provides config for the nutanix.disk.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.antivirus_server.count:
        description: "NutanixFilesAntivirusServerCountMetricConfig provides config for the nutanix.files.antivirus_server.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.antivirus_server.stat:
        description: "NutanixFilesAntivirusServerStatMetricConfig provides config for the nutanix.files.antivirus_server.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.file_server.count:
        description: "NutanixFilesFileServerCountMetricConfig provides config for the nutanix.files.file_server.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.file_server.stat:
        description: "NutanixFilesFileServerStatMetricConfig provides config for the nutanix.files.file_server.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.mount_target.count:
        description: "NutanixFilesMountTargetCountMetricConfig provides config for the nutanix.files.mount_target.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.mount_target.stat:
        description: "NutanixFilesMountTargetStatMetricConfig provides config for the nutanix.files.mount_target.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.files.unified_namespace.count:
        description: "NutanixFilesUnifiedNamespaceCountMetricConfig provides config for the nutanix.files.unified_namespace.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.host.count:
        description: "NutanixHostCountMetricConfig provides config for the nutanix.host.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.host.stat:
        description: "NutanixHostStatMetricConfig provides config for the nutanix.host.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.microseg.address_group.count:
        description: "NutanixMicrosegAddressGroupCountMetricConfig provides config for the nutanix.microseg.address_group.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.microseg.network_security_policy.count:
        description: "NutanixMicrosegNetworkSecurityPolicyCountMetricConfig provides config for the nutanix.microseg.network_security_policy.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.microseg.service_group.count:
        description: "NutanixMicrosegServiceGroupCountMetricConfig provides config for the nutanix.microseg.service_group.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.monitoring.alert.count:
        description: "NutanixMonitoringAlertCountMetricConfig provides config for the nutanix.monitoring.alert.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.bgp_session.count:
        description: "NutanixNetworkingBgpSessionCountMetricConfig provides config for the nutanix.networking.bgp_session.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.gateway.count:
        description: "NutanixNetworkingGatewayCountMetricConfig provides config for the nutanix.networking.gateway.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.layer2_stretch.count:
        description: "NutanixNetworkingLayer2StretchCountMetricConfig provides config for the nutanix.networking.layer2_stretch.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.layer2_stretch.stat:
        description: "NutanixNetworkingLayer2StretchStatMetricConfig provides config for the nutanix.networking.layer2_stretch.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.network_controller.count:
        description: "NutanixNetworkingNetworkControllerCountMetricConfig provides config for the nutanix.networking.network_controller.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.routing_policy.count:
        description: "NutanixNetworkingRoutingPolicyCountMetricConfig provides config for the nutanix.networking.routing_policy.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.traffic_mirror.count:
        description: "NutanixNetworkingTrafficMirrorCountMetricConfig provides config for the nutanix.networking.traffic_mirror.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.traffic_mirror.stat:
        description: "NutanixNetworkingTrafficMirrorStatMetricConfig provides config for the nutanix.networking.traffic_mirror.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.uplink_bond.count:
        description: "NutanixNetworkingUplinkBondCountMetricConfig provides config for the nutanix.networking.uplink_bond.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.virtual_switch.count:
        description: "NutanixNetworkingVirtualSwitchCountMetricConfig provides config for the nutanix.networking.virtual_switch.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.vpc.count:
        description: "NutanixNetworkingVpcCountMetricConfig provides config for the nutanix.networking.vpc.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.vpc.external_subnet.stat:
        description: "NutanixNetworkingVpcExternalSubnetStatMetricConfig provides config for the nutanix.networking.vpc.external_subnet.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.vpn_connection.count:
        description: "NutanixNetworkingVpnConnectionCountMetricConfig provides config for the nutanix.networking.vpn_connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.networking.vpn_connection.stat:
        description: "NutanixNetworkingVpnConnectionStatMetricConfig provides config for the nutanix.networking.vpn_connection.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.objects.object_store.count:
        description: "NutanixObjectsObjectStoreCountMetricConfig provides config for the nutanix.objects.object_store.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.objects.object_store.stat:
        description: "NutanixObjectsObjectStoreStatMetricConfig provides config for the nutanix.objects.object_store.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.prism.category.count:
        description: "NutanixPrismCategoryCountMetricConfig provides config for the nutanix.prism.category.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.prism.category_key.count:
        description: "NutanixPrismCategoryKeyCountMetricConfig provides config for the nutanix.prism.category_key.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.prism.task.count:
        description: "NutanixPrismTaskCountMetricConfig provides config for the nutanix.prism.task.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.storage.container.count:
        description: "NutanixStorageContainerCountMetricConfig provides config for the nutanix.storage.container.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.storage.container.stat:
        description: "NutanixStorageContainerStatMetricConfig provides config for the nutanix.storage.container.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.subnet.count:
        description: "NutanixSubnetCountMetricConfig provides config for the nutanix.subnet.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.count:
        description: "NutanixVMCountMetricConfig provides config for the nutanix.vm.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.disk.count:
        description: "NutanixVMDiskCountMetricConfig provides config for the nutanix.vm.disk.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.memory.assigned:
        description: "NutanixVMMemoryAssignedMetricConfig provides config for the nutanix.vm.memory.assigned metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.nics.count:
        description: "NutanixVMNicsCountMetricConfig provides config for the nutanix.vm.nics.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.stat:
        description: "NutanixVMStatMetricConfig provides config for the nutanix.vm.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.vm.vcpu.count:
        description: "NutanixVMVcpuCountMetricConfig provides config for the nutanix.vm.vcpu.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.volume_group.count:
        description: "NutanixVolumeGroupCountMetricConfig provides config for the nutanix.volume_group.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nutanix.volume_group.stat:
        description: "NutanixVolumeGroupStatMetricConfig provides config for the nutanix.volume_group.stat metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  resource_attributes_config:
    description: ResourceAttributesConfig provides config for nutanix resource attributes.
    type: object
    properties:
      nutanix.prism.api.version:
        description: ResourceAttributeConfig provides common config for a nutanix.prism.api.version resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      server.address:
        description: ResourceAttributeConfig provides common config for a server.address resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      server.port:
        description: ResourceAttributeConfig provides common config for a server.port resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.instance.id:
        description: ResourceAttributeConfig provides common config for a service.instance.id resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
      service.name:
        description: ResourceAttributeConfig provides common config for a service.name resource attribute.
        type: object
        properties:
          enabled:
            type: boolean
            default: true
          metrics_include:
            description: "Experimental: MetricsInclude defines a list of filters for attribute values. If the list is not empty, only metrics with matching resource attribute values will be emitted."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
          metrics_exclude:
            description: "Experimental: MetricsExclude defines a list of filters for attribute values. If the list is not empty, metrics with matching resource attribute values will not be emitted. MetricsInclude has higher priority than MetricsExclude."
            type: array
            items:
              $ref: go.opentelemetry.io/collector/filter.config
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for nutanix metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
      resource_attributes:
        $ref: resource_attributes_config
