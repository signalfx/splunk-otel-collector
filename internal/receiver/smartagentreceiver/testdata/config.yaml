receivers:
  smartagent:

processors:
  exampleprocessor:

exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [smartagent]
      processors: [exampleprocessor]
      exporters: [exampleexporter]
