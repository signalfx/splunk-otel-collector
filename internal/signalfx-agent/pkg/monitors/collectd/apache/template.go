//go:build linux
// +build linux

package apache

// AUTOGENERATED BY scripts/collectd-template-to-go.  DO NOT EDIT!!

import (
	"text/template"

	"github.com/signalfx/signalfx-agent/pkg/monitors/collectd"
)

// CollectdTemplate is a template for a apache collectd config file
var CollectdTemplate = template.Must(collectd.InjectTemplateFuncs(template.New("apache")).Parse(`
<LoadPlugin "apache">
  Interval {{.IntervalSeconds}}
</LoadPlugin>
<Plugin "apache">
  <Instance "{{.Name}}[monitorID={{.MonitorID}}]">
    {{with .URL}}
    URL "{{renderValue . $ }}"
    {{end}}
    {{with .Username -}}
    User "{{.}}"
    {{end}}
    {{with .Password}}
    Password "{{.}}"
    {{end}}
  </Instance>
</Plugin>
`)).Option("missingkey=error")
