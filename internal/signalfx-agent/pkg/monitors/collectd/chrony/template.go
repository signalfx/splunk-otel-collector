//go:build linux
// +build linux

package chrony

// AUTOGENERATED BY scripts/collectd-template-to-go.  DO NOT EDIT!!

import (
	"text/template"

	"github.com/signalfx/signalfx-agent/pkg/monitors/collectd"
)

// CollectdTemplate is a template for a chrony collectd config file
var CollectdTemplate = template.Must(collectd.InjectTemplateFuncs(template.New("chrony")).Parse(`
LoadPlugin chrony
<Plugin chrony>
  Host "{{.Host}}"
{{with .Port}}
  Port "{{.}}"
{{end}}
{{with .Timeout}}
  Timeout "{{.}}"
{{end}}
</Plugin>

<Chain "PostCache">
  <Rule "set_chrony_monitor_id">
    <Match "regex">
      Plugin "^chrony$"
    </Match>
    <Target "set">
      MetaData "monitorID" "{{.MonitorID}}"
    </Target>
  </Rule>
</Chain>
`)).Option("missingkey=error")
