[Sample_Addon://<name>]

everything_set = <value>
* SET ALL THE THINGS (Default: $SPLUNK_OTEL_TA_HOME/local/access_token )

minimal_set = <value>
* This is all you need

minimal_set_required = <value>
* hello

unary_flag_with_everything_set = <value>
* Unary flags don't take arguments/values and are either present or not (Default: $SPLUNK_OTEL_TA_HOME/local/access_token )
