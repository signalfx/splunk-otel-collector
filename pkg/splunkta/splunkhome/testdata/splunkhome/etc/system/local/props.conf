[syslog]
TRANSFORMS = syslog_transform
FIELDALIAS = src_ip as source_ip
FIELDALIAS = dst_ip as dest_ip

[tcp_source]
TIME_PREFIX = timestamp
TIME_FORMAT = %Y-%m-%d %H:%M:%S

[source::monitor:///var/log/bar with space]
TRUNCATE = 1024
CHARSET = UTF-8
