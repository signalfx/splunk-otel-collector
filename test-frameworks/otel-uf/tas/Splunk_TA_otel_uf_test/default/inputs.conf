# This is intentionally the only input in this test TA.
# add_ta.sh/install_{uf,otel}.sh replace __INDEX__ per destination:
#   VM A (UF):  uf_index
#   VM B (OTel): otel_index
[monitor:///home/splunker/otel_uf_test.log]
disabled = 0
index = __INDEX__
sourcetype = otel_uf_test
source = otel_uf_test

