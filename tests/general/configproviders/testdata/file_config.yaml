receivers:
  host_metrics: ${file:./testdata/file_with_config_content}
exporters:
  otlp_grpc:
    endpoint: ${env:OTLP_ENDPOINT}
    tls:
      insecure: true

service:
  pipelines:
    metrics:
      receivers: [host_metrics]
      exporters: [otlp_grpc]